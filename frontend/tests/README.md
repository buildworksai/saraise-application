# frontend/tests/

Purpose
- Frontend tests (Vitest + Testing Library).

Conventions
- Keep tests fast and deterministic.
- Prefer component tests that validate accessibility semantics and critical user flows.
