import{useMutation,useQueryClient}from'@tanstack/react-query';import{useNavigate}from'react-router-dom';import{Card,CardContent}from'@/components/ui/Card';import{CrmPage}from'../components/CrmPage';import{LeadForm}from'../forms';import{CrmApiError,crmKeys,crmService}from'../services/crm-service';
export function LeadCreatePage(){const nav=useNavigate(),qc=useQueryClient();const m=useMutation({mutationFn:crmService.createLead,onSuccess:async lead=>{await qc.invalidateQueries({queryKey:crmKeys.all});nav(`/crm/leads/${lead.id}`);}});return <CrmPage title="Create lead" description="Start with only the prospect details you know." parent={{label:'Leads',to:'/crm/leads'}}><Card><CardContent className="pt-6"><LeadForm pending={m.isPending} serverError={m.error instanceof CrmApiError?(m.error.code.includes('duplicate')?'A lead with this email already exists. Review the existing record.':m.error.message):null} onSubmit={async p=>{await m.mutateAsync(p)}}/></CardContent></Card></CrmPage>}
