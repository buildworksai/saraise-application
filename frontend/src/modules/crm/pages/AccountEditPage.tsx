import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{useNavigate,useParams}from'react-router-dom';import{Card,CardContent}from'@/components/ui/Card';import{CrmPage,GovernedError,PageSkeleton}from'../components/CrmPage';import{AccountForm}from'../forms';import{CrmApiError,crmKeys,crmService}from'../services/crm-service';
export function AccountEditPage(){const{id=''}=useParams(),nav=useNavigate(),qc=useQueryClient();const q=useQuery({queryKey:crmKeys.account(id),queryFn:()=>crmService.getAccount(id),enabled:!!id});const m=useMutation({mutationFn:(p:Parameters<typeof crmService.updateAccount>[1])=>crmService.updateAccount(id,p),onSuccess:async a=>{await qc.invalidateQueries({queryKey:crmKeys.account(id)});nav(`/crm/accounts/${a.id}`)}});return <CrmPage title="Edit account" description="Hierarchy depth and duplicate checks are enforced on save." parent={{label:'Accounts',to:'/crm/accounts'}}>{q.isLoading?<PageSkeleton/>:q.error||!q.data?<GovernedError error={q.error} onRetry={()=>void q.refetch()} subject="Account"/>:<Card><CardContent className="pt-6"><AccountForm initial={q.data} pending={m.isPending} serverError={m.error instanceof CrmApiError?(m.error.kind==='conflict'?'This account changed. Reload the latest version.':m.error.message):null} onSubmit={async p=>{await m.mutateAsync({...p,version:q.data.version})}}/></CardContent></Card>}</CrmPage>}
