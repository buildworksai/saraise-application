/* eslint-disable @typescript-eslint/no-unused-vars -- account is immutable through opportunity PATCH */
import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{useNavigate,useParams}from'react-router-dom';import{Card,CardContent}from'@/components/ui/Card';import{CrmPage,GovernedError,PageSkeleton}from'../components/CrmPage';import{OpportunityForm}from'../forms';import{CrmApiError,crmKeys,crmService}from'../services/crm-service';
export function OpportunityEditPage(){const{id=''}=useParams(),nav=useNavigate(),qc=useQueryClient();const q=useQuery({queryKey:crmKeys.opportunity(id),queryFn:()=>crmService.getOpportunity(id),enabled:!!id});const m=useMutation({mutationFn:(p:Parameters<typeof crmService.updateOpportunity>[1])=>crmService.updateOpportunity(id,p),onSuccess:async o=>{await qc.invalidateQueries({queryKey:crmKeys.opportunity(id)});nav(`/crm/opportunities/${o.id}`)}});return <CrmPage title="Edit opportunity" description="Pipeline stage and status remain protected by transition commands." parent={{label:'Opportunities',to:'/crm/opportunities'}}>{q.isLoading?<PageSkeleton/>:q.error||!q.data?<GovernedError error={q.error} onRetry={()=>void q.refetch()} subject="Opportunity"/>:<Card><CardContent className="pt-6"><OpportunityForm initial={q.data} pending={m.isPending} serverError={m.error instanceof CrmApiError?(m.error.kind==='conflict'?'This opportunity changed. Reload before saving.':m.error.message):null} onSubmit={async({account_id:_,...p})=>{await m.mutateAsync({...p,version:q.data.version})}}/></CardContent></Card>}</CrmPage>}
