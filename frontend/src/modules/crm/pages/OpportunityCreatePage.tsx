/* eslint-disable @typescript-eslint/prefer-nullish-coalescing -- empty form values intentionally fall back */
import{useMutation,useQueryClient}from'@tanstack/react-query';import{useNavigate,useSearchParams}from'react-router-dom';import{Card,CardContent}from'@/components/ui/Card';import{CrmPage}from'../components/CrmPage';import{OpportunityForm}from'../forms';import{CrmApiError,crmKeys,crmService}from'../services/crm-service';
export function OpportunityCreatePage(){const nav=useNavigate(),qc=useQueryClient(),[params]=useSearchParams();const m=useMutation({mutationFn:crmService.createOpportunity,onSuccess:async o=>{await qc.invalidateQueries({queryKey:crmKeys.all});nav(`/crm/opportunities/${o.id}`)}});return <CrmPage title="Create opportunity" description="Connect an account, optional account contact, value, and expected close date." parent={{label:'Opportunities',to:'/crm/opportunities'}}><Card><CardContent className="pt-6"><OpportunityForm pending={m.isPending} serverError={m.error instanceof CrmApiError?m.error.message:null} onSubmit={async p=>{await m.mutateAsync({...p,account_id:p.account_id||params.get('account_id')||''})}}/></CardContent></Card></CrmPage>}
