/* eslint-disable @typescript-eslint/prefer-nullish-coalescing -- empty form values intentionally fall back */
import{useMutation,useQueryClient}from'@tanstack/react-query';import{useNavigate,useSearchParams}from'react-router-dom';import{Card,CardContent}from'@/components/ui/Card';import{CrmPage}from'../components/CrmPage';import{ContactForm}from'../forms';import{CrmApiError,crmKeys,crmService}from'../services/crm-service';
export function ContactCreatePage(){const nav=useNavigate(),qc=useQueryClient(),[params]=useSearchParams();const m=useMutation({mutationFn:crmService.createContact,onSuccess:async c=>{await qc.invalidateQueries({queryKey:crmKeys.all});nav(`/crm/contacts/${c.id}`)}});const error=m.error instanceof CrmApiError?m.error:null;return <CrmPage title="Create contact" description="Contacts must belong to an account; email-domain rules protect relationship quality." parent={{label:'Contacts',to:'/crm/contacts'}}><Card><CardContent className="pt-6"><ContactForm pending={m.isPending} serverError={error?(error.code.includes('domain')?'Email domain differs from the account. Ask an authorized user to provide an override reason.':error.code.includes('duplicate')?'That account already has a contact with this email.':error.message):null} onSubmit={async p=>{await m.mutateAsync({...p,account_id:p.account_id||params.get('account_id')||''})}}/></CardContent></Card></CrmPage>}
