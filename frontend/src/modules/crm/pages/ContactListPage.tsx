/* eslint-disable @typescript-eslint/prefer-nullish-coalescing */
import { useQuery } from '@tanstack/react-query';import { Link,useSearchParams } from 'react-router-dom';import { CrmPage,GovernedError,PageSkeleton } from '../components/CrmPage';import { EntityList } from '../components/EntityList';import { useCrmConfiguration } from '../hooks/use-crm-configuration';import { crmKeys,crmService } from '../services/crm-service';import type { ContactFilters } from '../contracts';
export function ContactListPage(){const[p]=useSearchParams(),configuration=useCrmConfiguration(),document=configuration.data?.document;const filters:ContactFilters={search:p.get('search')||undefined,account_id:p.get('account_id')||undefined,owner_id:p.get('owner_id')||undefined,engagement_min:p.get('engagement_min')?Number(p.get('engagement_min')):undefined,page:Number(p.get('page')||1),page_size:document?Number(p.get('page_size')||document.ui.saved_page_size):undefined,ordering:p.get('ordering')||undefined};const q=useQuery({queryKey:crmKeys.contacts(filters),queryFn:()=>crmService.listContacts(filters),enabled:!!document});if(configuration.isLoading)return <CrmPage title="Contacts"><PageSkeleton label="Loading contact configuration"/></CrmPage>;if(configuration.error||!document)return <CrmPage title="Contacts"><GovernedError error={configuration.error} onRetry={()=>void configuration.refetch()} subject="Contact configuration"/></CrmPage>;return <EntityList title="Contacts" description="See the people, relationships, and engagement evidence behind every deal." createPath="/crm/contacts/new" emptyTitle="Create your first contact" emptyDescription="Add a contact to an existing account to begin tracking engagement." query={q.data} isLoading={q.isLoading} error={q.error} refetch={()=>void q.refetch()} filters={[{key:'account_id',label:'Account ID'},{key:'owner_id',label:'Owner ID'},{key:'engagement_min',label:`Minimum engagement (${document.contact.engagement_score_min}-${document.contact.engagement_score_max})`}]} columns={[{key:'last_name',label:'Name',sortable:true,render:contact=><Link className="font-medium text-primary hover:underline" to={`/crm/contacts/${contact.id}`}>{contact.first_name} {contact.last_name}</Link>},{key:'email',label:'Email',render:contact=>contact.email||'—'},{key:'title',label:'Title',render:contact=>contact.title||'—'},{key:'engagement_score',label:'Engagement',sortable:true,render:contact=>String(contact.engagement_score)},{key:'last_contacted_at',label:'Last contacted',render:contact=>contact.last_contacted_at?new Date(contact.last_contacted_at).toLocaleDateString():'No evidence yet'}]}/>}
