/* eslint-disable @typescript-eslint/prefer-nullish-coalescing -- empty query values intentionally mean unset */
import{useQuery}from'@tanstack/react-query';import{Link,useSearchParams}from'react-router-dom';import{EntityList}from'../components/EntityList';import{crmKeys,crmService}from'../services/crm-service';import type{OpportunityFilters}from'../contracts';
export function OpportunityListPage(){const[p]=useSearchParams();const f:OpportunityFilters={search:p.get('search')||undefined,status:(p.get('status')as OpportunityFilters['status'])||undefined,stage:(p.get('stage')as OpportunityFilters['stage'])||undefined,account_id:p.get('account_id')||undefined,close_date_from:p.get('close_date_from')||undefined,close_date_to:p.get('close_date_to')||undefined,page:Number(p.get('page')||1),ordering:p.get('ordering')||undefined};const q=useQuery({queryKey:crmKeys.opportunities(f),queryFn:()=>crmService.listOpportunities(f)});const totals=q.data?.items.reduce<Record<string,number>>((sum,o)=>({...sum,[o.currency]:(sum[o.currency]??0)+Number(o.amount)}),{});return <EntityList title="Opportunities" description="A currency-safe view of open and closed revenue." createPath="/crm/opportunities/new" emptyTitle="Create your first opportunity" emptyDescription="Connect a qualified deal to an account and expected close date." query={q.data} isLoading={q.isLoading} error={q.error} refetch={()=>void q.refetch()} total={totals?<div className="flex flex-wrap gap-2">{Object.entries(totals).map(([currency,amount])=><span key={currency} className="rounded-full bg-muted px-3 py-1 text-sm">Page total: {new Intl.NumberFormat(undefined,{style:'currency',currency}).format(amount)}</span>)}</div>:null} filters={[{key:'status',label:'Status',choices:['open','won','lost'].map(v=>({value:v,label:v}))},{key:'stage',label:'Stage',choices:['prospecting','qualification','needs_analysis','proposal','negotiation','closed_won','closed_lost'].map(v=>({value:v,label:v.replaceAll('_',' ')}))},{key:'account_id',label:'Account ID'},{key:'close_date_from',label:'Close from'},{key:'close_date_to',label:'Close to'}]} columns={[{key:'name',label:'Name',sortable:true,render:o=><Link className="font-medium text-primary hover:underline" to={`/crm/opportunities/${o.id}`}>{o.name}</Link>},{key:'stage',label:'Stage',render:o=>o.stage.replaceAll('_',' ')},{key:'amount',label:'Amount',sortable:true,render:o=>new Intl.NumberFormat(undefined,{style:'currency',currency:o.currency}).format(Number(o.amount))},{key:'probability',label:'Probability',render:o=>`${o.probability}%`},{key:'close_date',label:'Close date',sortable:true,render:o=>new Date(`${o.close_date}T00:00:00`).toLocaleDateString()},{key:'status',label:'Status',render:o=>o.status}]}/>}
