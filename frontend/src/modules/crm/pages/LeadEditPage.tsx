import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{useNavigate,useParams}from'react-router-dom';import{Card,CardContent}from'@/components/ui/Card';import{CrmPage,GovernedError,PageSkeleton}from'../components/CrmPage';import{LeadForm}from'../forms';import{CrmApiError,crmKeys,crmService}from'../services/crm-service';
export function LeadEditPage(){const{id=''}=useParams(),nav=useNavigate(),qc=useQueryClient();const q=useQuery({queryKey:crmKeys.lead(id),queryFn:()=>crmService.getLead(id),enabled:!!id});const m=useMutation({mutationFn:(p:Parameters<typeof crmService.updateLead>[1])=>crmService.updateLead(id,p),onSuccess:async lead=>{await qc.invalidateQueries({queryKey:crmKeys.lead(id)});nav(`/crm/leads/${lead.id}`)}});return <CrmPage title="Edit lead" description="Lifecycle state is changed only through qualified CRM commands." parent={{label:'Leads',to:'/crm/leads'}}>{q.isLoading?<PageSkeleton/>:q.error||!q.data?<GovernedError error={q.error} onRetry={()=>void q.refetch()} subject="Lead"/>:<Card><CardContent className="pt-6"><LeadForm initial={q.data} pending={m.isPending} serverError={m.error instanceof CrmApiError?(m.error.kind==='conflict'?'This lead changed. Reload before saving again.':m.error.message):null} onSubmit={async p=>{await m.mutateAsync({...p,version:q.data.version})}}/></CardContent></Card>}</CrmPage>}
