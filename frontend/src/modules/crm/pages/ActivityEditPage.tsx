/* eslint-disable @typescript-eslint/no-unused-vars -- excluded relationship fields must never reach PATCH */
import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{useNavigate,useParams}from'react-router-dom';import{Card,CardContent}from'@/components/ui/Card';import{CrmPage,GovernedError,PageSkeleton}from'../components/CrmPage';import{ActivityForm}from'../forms';import{CrmApiError,crmKeys,crmService}from'../services/crm-service';
export function ActivityEditPage(){const{id=''}=useParams(),nav=useNavigate(),qc=useQueryClient();const q=useQuery({queryKey:crmKeys.activity(id),queryFn:()=>crmService.getActivity(id),enabled:!!id});const m=useMutation({mutationFn:(p:Parameters<typeof crmService.updateActivity>[1])=>crmService.updateActivity(id,p),onSuccess:async a=>{await qc.invalidateQueries({queryKey:crmKeys.activity(id)});nav(`/crm/activities/${a.id}`)}});return <CrmPage title="Edit activity" description="Completed activities are audit evidence and remain read-only." parent={{label:'Activities',to:'/crm/activities'}}>{q.isLoading?<PageSkeleton/>:q.error||!q.data?<GovernedError error={q.error} onRetry={()=>void q.refetch()} subject="Activity"/>:<Card><CardContent className="pt-6"><ActivityForm initial={q.data} pending={m.isPending} serverError={m.error instanceof CrmApiError?(m.error.kind==='conflict'?'This activity changed. Reload before saving.':m.error.message):null} onSubmit={async({related_to_id:_,related_to_type:__,...p})=>{await m.mutateAsync({...p,version:q.data.version})}}/></CardContent></Card>}</CrmPage>}
