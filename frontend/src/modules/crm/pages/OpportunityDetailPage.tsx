/* eslint-disable @typescript-eslint/no-base-to-string */
import{useState}from'react';import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{Link,useParams}from'react-router-dom';import{Button}from'@/components/ui/Button';import{Dialog}from'@/components/ui/Dialog';import{CrmPage,Field,GovernedError,LiveRegion,PageSkeleton,fieldClass}from'../components/CrmPage';import{DetailCard}from'../components/DetailCard';import{ActivityTimeline}from'../components/ActivityTimeline';import{crmKeys,crmService}from'../services/crm-service';
export function OpportunityDetailPage(){const{id=''}=useParams(),qc=useQueryClient();const[won,setWon]=useState(false),[lost,setLost]=useState(false);const q=useQuery({queryKey:crmKeys.opportunity(id),queryFn:()=>crmService.getOpportunity(id),enabled:!!id});const activities=useQuery({queryKey:crmKeys.activities({related_to_type:'Opportunity',related_to_id:id,page:1}),queryFn:()=>crmService.listActivities({related_to_type:'Opportunity',related_to_id:id,page:1}),enabled:!!id});const done=async()=>{await qc.invalidateQueries({queryKey:crmKeys.opportunity(id)});await qc.invalidateQueries({queryKey:['crm','opportunities']});setWon(false);setLost(false)};const closeWon=useMutation({mutationFn:()=>crmService.closeOpportunityWon(id,{expected_version:q.data?.version??0,transition_key:`close-won-${crypto.randomUUID()}`,confirmation:true}),onSuccess:done});const closeLost=useMutation({mutationFn:(reason:string)=>crmService.closeOpportunityLost(id,{expected_version:q.data?.version??0,transition_key:`close-lost-${crypto.randomUUID()}`,loss_reason:reason}),onSuccess:done});if(q.isLoading)return <CrmPage title="Opportunity" parent={{label:'Opportunities',to:'/crm/opportunities'}}><PageSkeleton/></CrmPage>;if(q.error||!q.data)return <CrmPage title="Opportunity" parent={{label:'Opportunities',to:'/crm/opportunities'}}><GovernedError error={q.error} onRetry={()=>void q.refetch()} subject="Opportunity"/></CrmPage>;const o=q.data;return <CrmPage title={o.name} description={`${o.stage.replaceAll('_',' ')} · ${o.status}`} parent={{label:'Opportunities',to:'/crm/opportunities'}} actions={<><Link to={`/crm/opportunities/${o.id}/edit`}><Button variant="outline">Edit</Button></Link>{o.status==='open'?<><Button onClick={()=>setWon(true)}>Close won</Button><Button variant="outline" onClick={()=>setLost(true)}>Close lost</Button></>:null}<Link to={`/crm/activities/new?related_to_type=Opportunity&related_to_id=${o.id}`}><Button variant="outline">Log activity</Button></Link></>}><LiveRegion message={closeWon.isSuccess?'Opportunity closed won.':closeLost.isSuccess?'Opportunity closed lost.':null}/><div className="grid gap-6 lg:grid-cols-3"><div className="space-y-6 lg:col-span-2"><DetailCard title="Deal information" fields={[{label:'Account',value:<Link className="text-primary hover:underline" to={`/crm/accounts/${o.account_id}`}>Open account</Link>},{label:'Primary contact',value:o.primary_contact_id?<Link className="text-primary hover:underline" to={`/crm/contacts/${o.primary_contact_id}`}>Open contact</Link>:'—'},{label:'Amount',value:new Intl.NumberFormat(undefined,{style:'currency',currency:o.currency}).format(Number(o.amount))},{label:'Probability',value:`${o.probability}%`},{label:'Expected close',value:new Date(`${o.close_date}T00:00:00`).toLocaleDateString()},{label:'Description',value:o.description||'—'}]}/><DetailCard title="Activity timeline"><ActivityTimeline activities={activities.data?.items} loading={activities.isLoading} error={activities.error} onRetry={()=>void activities.refetch()}/></DetailCard></div><DetailCard title="Stage history">{o.transition_history.length?<ol className="space-y-3">{o.transition_history.map(t=><li key={t.key} className="border-l-2 pl-3 text-sm"><strong>{t.from_state} → {t.to_state}</strong><p className="text-muted-foreground">{new Date(t.occurred_at).toLocaleString()}{t.reason?` · ${t.reason}`:''}</p></li>)}</ol>:<p className="text-sm text-muted-foreground">No stage transitions recorded.</p>}</DetailCard></div>
<Dialog open={won} onOpenChange={setWon} title="Confirm closed won" description="This terminal transition fixes probability at 100% and may notify entitled fulfillment extensions."><div className="flex justify-end gap-2"><Button variant="outline" onClick={()=>setWon(false)}>Cancel</Button><Button disabled={closeWon.isPending} onClick={()=>closeWon.mutate()}>{closeWon.isPending?'Closing…':'Confirm won'}</Button></div>{closeWon.error?<GovernedError error={closeWon.error} subject="Close won"/>:null}</Dialog>
<Dialog open={lost} onOpenChange={setLost} title="Close opportunity as lost" description="A meaningful reason is retained in the transition audit trail."><form onSubmit={e=>{e.preventDefault();closeLost.mutate(String(new FormData(e.currentTarget).get('reason')).trim())}} className="space-y-4"><Field id="reason" label="Loss reason" required><textarea id="reason" name="reason" required minLength={2} rows={4} className={fieldClass}/></Field>{closeLost.error?<GovernedError error={closeLost.error} subject="Close lost"/>:null}<div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={()=>setLost(false)}>Cancel</Button><Button type="submit" variant="danger" disabled={closeLost.isPending}>{closeLost.isPending?'Closing…':'Confirm lost'}</Button></div></form></Dialog></CrmPage>}
