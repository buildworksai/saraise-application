/* eslint-disable @typescript-eslint/no-unused-vars -- account is immutable through contact PATCH */
import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{useNavigate,useParams}from'react-router-dom';import{Card,CardContent}from'@/components/ui/Card';import{CrmPage,GovernedError,PageSkeleton}from'../components/CrmPage';import{ContactForm}from'../forms';import{CrmApiError,crmKeys,crmService}from'../services/crm-service';
export function ContactEditPage(){const{id=''}=useParams(),nav=useNavigate(),qc=useQueryClient();const q=useQuery({queryKey:crmKeys.contact(id),queryFn:()=>crmService.getContact(id),enabled:!!id});const m=useMutation({mutationFn:(p:Parameters<typeof crmService.updateContact>[1])=>crmService.updateContact(id,p),onSuccess:async c=>{await qc.invalidateQueries({queryKey:crmKeys.contact(id)});nav(`/crm/contacts/${c.id}`)}});return <CrmPage title="Edit contact" description="Authorized domain overrides require an explicit audit reason." parent={{label:'Contacts',to:'/crm/contacts'}}>{q.isLoading?<PageSkeleton/>:q.error||!q.data?<GovernedError error={q.error} onRetry={()=>void q.refetch()} subject="Contact"/>:<Card><CardContent className="pt-6"><ContactForm initial={q.data} allowDomainOverride pending={m.isPending} serverError={m.error instanceof CrmApiError?(m.error.kind==='conflict'?'This contact changed. Reload before saving.':m.error.message):null} onSubmit={async({account_id:_,...p})=>{await m.mutateAsync({...p,version:q.data.version})}}/></CardContent></Card>}</CrmPage>}
