/* eslint-disable @typescript-eslint/prefer-nullish-coalescing -- empty query values intentionally mean unset */
import{useMutation,useQueryClient}from'@tanstack/react-query';import{useNavigate,useSearchParams}from'react-router-dom';import{Card,CardContent}from'@/components/ui/Card';import{CrmPage}from'../components/CrmPage';import{ActivityForm}from'../forms';import{CrmApiError,crmKeys,crmService}from'../services/crm-service';import type{RelatedEntityType}from'../contracts';
export function ActivityCreatePage(){const nav=useNavigate(),qc=useQueryClient(),[params]=useSearchParams();const m=useMutation({mutationFn:crmService.createActivity,onSuccess:async a=>{await qc.invalidateQueries({queryKey:crmKeys.all});nav(`/crm/activities/${a.id}`)}});const type=params.get('related_to_type');const preselected={related_to_type:type&&['Lead','Contact','Account','Opportunity'].includes(type)?type as RelatedEntityType:undefined,related_to_id:params.get('related_to_id')||undefined};return <CrmPage title="Log activity" description="Attach evidence to a CRM record; new task due dates must be in the future." parent={{label:'Activities',to:'/crm/activities'}}><Card><CardContent className="pt-6"><ActivityForm preselected={preselected} pending={m.isPending} serverError={m.error instanceof CrmApiError?m.error.message:null} onSubmit={async p=>{await m.mutateAsync(p)}}/></CardContent></Card></CrmPage>}
