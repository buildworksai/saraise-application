import type{StageForecast}from'../contracts';
import{useCrmConfiguration}from'../hooks/use-crm-configuration';
import{GovernedError,PageSkeleton}from'./CrmPage';
export function WeightedPipelineChart({data}:{data:readonly StageForecast[]}){const configuration=useCrmConfiguration();if(configuration.isLoading)return <PageSkeleton label="Loading pipeline chart configuration"/>;if(configuration.error||!configuration.data)return <GovernedError error={configuration.error} onRetry={()=>void configuration.refetch()} subject="Pipeline chart configuration"/>;if(!data.length)return <p className="rounded border border-dashed p-6 text-center text-sm text-muted-foreground">No pipeline evidence for this period.</p>;const minimum=configuration.data.document.ui.minimum_pipeline_bar_percent;const maximum=Math.max(...data.map(row=>Number(row.weighted_value)),1);return <div role="img" aria-label="Weighted pipeline by stage and currency" className="space-y-4">{data.map(row=><div key={`${row.stage}-${row.currency}`}><div className="mb-1 flex justify-between text-sm"><span>{row.stage.replaceAll('_',' ')} · {row.currency}</span><span>{new Intl.NumberFormat(undefined,{style:'currency',currency:row.currency}).format(Number(row.weighted_value))}</span></div><div className="h-3 overflow-hidden rounded bg-muted"><div className="h-full rounded bg-primary" style={{width:`${Math.max(minimum,Number(row.weighted_value)/maximum*100)}%`}}/></div></div>)}</div>}
