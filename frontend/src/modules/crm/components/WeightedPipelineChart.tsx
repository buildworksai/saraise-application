import type{StageForecast}from'../contracts';
export function WeightedPipelineChart({data}:{data:readonly StageForecast[]}){if(!data.length)return <p className="rounded border border-dashed p-6 text-center text-sm text-muted-foreground">No pipeline evidence for this period.</p>;const maximum=Math.max(...data.map(row=>Number(row.weighted_value)),1);return <div role="img" aria-label="Weighted pipeline by stage and currency" className="space-y-4">{data.map(row=><div key={`${row.stage}-${row.currency}`}><div className="mb-1 flex justify-between text-sm"><span>{row.stage.replaceAll('_',' ')} · {row.currency}</span><span>{new Intl.NumberFormat(undefined,{style:'currency',currency:row.currency}).format(Number(row.weighted_value))}</span></div><div className="h-3 overflow-hidden rounded bg-muted"><div className="h-full rounded bg-primary" style={{width:`${Math.max(2,Number(row.weighted_value)/maximum*100)}%`}}/></div></div>)}</div>}
