# Self-Hosted Licensing Module - Runtime Plane Rules

# Control Plane settings, feature flags, metrics, health, and audit UI are
# forbidden here. This directory may contain only self-hosted license status
# and activation, gated on VITE_SARAISE_MODE=self-hosted.

# Read contracts.ts first and use its exported types and ENDPOINTS.
# Validate every edit with TypeScript and ESLint at zero errors/warnings.
