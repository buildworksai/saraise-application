import { useQuery } from '@tanstack/react-query'; import { LockKeyhole } from 'lucide-react'; import { useParams } from 'react-router-dom'; import { Card } from '@/components/ui/Card';
import { ApiProblem, PageHeader, PageSkeleton } from '../components/ModuleShell'; import { formatDate } from '../components/utils'; import { processMiningService } from '../services/process_mining-service';

export function EventDetailPage() { const { id = '' } = useParams(); const query = useQuery({ queryKey: ['process-mining', 'event', id], queryFn: () => processMiningService.getEvent(id), enabled: Boolean(id) }); if (query.isLoading) return <PageSkeleton table={false}/>; if (query.error || !query.data) return <main className="p-4 sm:p-8"><ApiProblem error={query.error} onRetry={() => void query.refetch()}/></main>; const item = query.data; return <main className="space-y-6 p-4 sm:p-8"><PageHeader title={item.activity} description="Immutable canonical event evidence. Sensitive classified attributes are redacted at the API boundary." actions={<span className="flex items-center gap-2 rounded-full bg-muted px-3 py-2 text-xs"><LockKeyhole className="h-4 w-4"/>Append-only</span>}/><Card className="p-6"><dl className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{[['Case', item.case_id], ['Process', item.process_name], ['Occurred', formatDate(item.occurred_at)], ['Resource', item.resource ?? '—'], ['Source', item.source_module], ['Source event', item.source_event_id ?? '—'], ['Ingested', formatDate(item.ingested_at)]].map(([label, value]) => <div key={label}><dt className="text-xs uppercase text-muted-foreground">{label}</dt><dd className="mt-1 break-all text-sm font-medium">{value}</dd></div>)}</dl></Card><Card className="p-6"><h2 className="font-semibold">Redacted attributes</h2><pre className="mt-4 overflow-auto rounded bg-muted p-4 text-xs">{JSON.stringify(item.attributes ?? {}, null, 2)}</pre></Card></main>; }
