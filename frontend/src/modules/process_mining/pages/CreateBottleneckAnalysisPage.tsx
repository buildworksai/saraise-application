import { useMutation, useQuery } from '@tanstack/react-query'; import { useEffect, useState } from 'react'; import { useNavigate, useSearchParams } from 'react-router-dom'; import { Button } from '@/components/ui/Button'; import { Card } from '@/components/ui/Card'; import { Input } from '@/components/ui/Input';
import { ApiProblem, PageHeader, PageSkeleton } from '../components/ModuleShell'; import { deterministicKey, toLocalInput } from '../components/utils'; import { PROCESS_MINING_ROUTES } from '../contracts'; import { processMiningService } from '../services/process_mining-service';
export function CreateBottleneckAnalysisPage() { const navigate = useNavigate(); const [params] = useSearchParams(); const [processName, setProcessName] = useState(params.get('process_name') ?? ''); const [start, setStart] = useState(''); const [end, setEnd] = useState(''); const configuration = useQuery({ queryKey: ['process-mining', 'configuration'], queryFn: processMiningService.getConfiguration }); useEffect(() => { if (configuration.data && !start) { const now = new Date(); setStart(toLocalInput(new Date(now.getTime() - configuration.data.document.default_time_window_days * 86400000))); setEnd(toLocalInput(now)); } }, [configuration.data, start]); const preview = useQuery({ queryKey: ['process-mining', 'bottleneck-preview', processName], queryFn: () => processMiningService.listProcesses({ process_name: processName, page_size: 1 }), enabled: Boolean(processName.trim()) }); const mutation = useMutation({ mutationFn: () => processMiningService.createBottleneck({ process_name: processName.trim(), time_range_start: new Date(start).toISOString(), time_range_end: new Date(end).toISOString(), idempotency_key: deterministicKey('bottleneck', processName, start, end) }), onSuccess: (analysis) => navigate(PROCESS_MINING_ROUTES.BOTTLENECK(analysis.id)) }); if (!configuration.data) return <PageSkeleton/>; const minimum = configuration.data.document.bottleneck_min_cases; const process = preview.data?.items[0]; const eligible = Boolean(process && process.case_count >= minimum && new Date(end) > new Date(start)); return <main className="space-y-6 p-4 sm:p-8"><PageHeader title="Create bottleneck analysis" description="Preview configured case coverage and bounded compute."/>{mutation.error && <ApiProblem error={mutation.error} onRetry={() => mutation.reset()}/>}<Card className="mx-auto max-w-3xl p-6"><form className="space-y-5" onSubmit={(event) => { event.preventDefault(); mutation.mutate(); }}><Input id="process" label="Process name" value={processName} onChange={(event) => setProcessName(event.target.value)} required/><div className="grid gap-4 sm:grid-cols-2"><Input id="start" label="Window start" type="datetime-local" value={start} onChange={(event) => setStart(event.target.value)} required/><Input id="end" label="Window end" type="datetime-local" value={end} onChange={(event) => setEnd(event.target.value)} required/></div><p className={process && process.case_count < minimum ? 'text-destructive' : 'text-muted-foreground'}>{process ? `${process.case_count} cases · configured minimum ${minimum}` : 'Enter an existing process name.'}</p><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={() => navigate(PROCESS_MINING_ROUTES.BOTTLENECKS)}>Cancel</Button><Button type="submit" disabled={!eligible || mutation.isPending}>Queue analysis</Button></div></form></Card></main>; }
