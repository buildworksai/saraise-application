import { useQuery } from "@tanstack/react-query"; import { Link, useNavigate, useSearchParams } from "react-router-dom"; import { Button } from "@/components/ui/Button"; import { ROUTES } from "../contracts"; import { masterDataService } from "../services/master-data-service"; import { EmptyState, GovernedError, PageHeader, PageSkeleton, Pagination, QUERY_KEYS, StatusPill } from "../components/MdmUI";
export function MatchingRuleListPage() { const navigate = useNavigate(); const [params, setParams] = useSearchParams(); const page = Number(params.get("page") ?? 1); const filters = { page, page_size: 25, ordering: "name" }; const query = useQuery({ queryKey: QUERY_KEYS.matchingRules(filters), queryFn: () => masterDataService.matchingRules.list(filters) }); if (query.isLoading) return <PageSkeleton/>; if (query.error) return <GovernedError error={query.error} retry={() => void query.refetch()}/>; if (!query.data) return <GovernedError error={new Error("No matching-rule response was received.")}/>; return <main className="space-y-6"><PageHeader title="Matching rules" description="Deterministic duplicate strategies with explicit blocking, weights, and thresholds." actions={<Button onClick={() => navigate(ROUTES.MATCHING_RULE_NEW)}>New matching rule</Button>}/>{query.data.items.length ? <section className="overflow-hidden rounded-xl border bg-card"><div className="divide-y">{query.data.items.map((rule) => <Link key={rule.id} to={ROUTES.MATCHING_RULE_DETAIL(rule.id)} className="grid gap-3 p-4 hover:bg-muted/30 md:grid-cols-[1fr_160px_200px_100px]"><span><strong>{rule.name}</strong><small className="block text-muted-foreground">{rule.entity_type_key ?? rule.entity_type}</small></span><span>{rule.algorithm}</span><span className="text-sm">Review ≥ {Number(rule.review_threshold).toFixed(2)}<small className="block text-muted-foreground">Confirm ≥ {Number(rule.auto_confirm_threshold).toFixed(2)}</small></span><StatusPill value={rule.is_active ? "active" : "inactive"}/></Link>)}</div><Pagination value={query.data.pagination} onPage={(next) => setParams({ page: String(next) })}/></section> : <EmptyState title="No matching rules" description="Duplicate scans remain unavailable until a deterministic rule is configured." action={<Button onClick={() => navigate(ROUTES.MATCHING_RULE_NEW)}>Create rule</Button>}/>}</main>; }
