import { useQuery } from "@tanstack/react-query";
import { Link, useNavigate, useSearchParams } from "react-router-dom";
import { Button } from "@/components/ui/Button";
import { ROUTES } from "../contracts";
import { masterDataService } from "../services/master-data-service";
import { EmptyState, GovernedError, PageHeader, PageSkeleton, Pagination, QUERY_KEYS, SeverityPill, StatusPill } from "../components/MdmUI";
export function QualityRuleListPage() { const navigate = useNavigate(); const [params, setParams] = useSearchParams(); const page = Number(params.get("page") ?? 1); const filters = { page, page_size: 25, ordering: "entity_type,name" }; const query = useQuery({ queryKey: QUERY_KEYS.qualityRules(filters), queryFn: () => masterDataService.qualityRules.list(filters) }); if (query.isLoading) return <PageSkeleton/>; if (query.error) return <GovernedError error={query.error} retry={() => void query.refetch()}/>; if (!query.data) return <GovernedError error={new Error("No quality-rule response was received.")}/>; return <main className="space-y-6"><PageHeader title="Quality rules" description="Deterministic, weighted validation rules that produce explainable scores and issues." actions={<Button onClick={() => navigate(ROUTES.QUALITY_RULE_NEW)}>New quality rule</Button>}/>{query.data.items.length ? <section className="overflow-hidden rounded-xl border bg-card"><div className="divide-y">{query.data.items.map((rule) => <Link key={rule.id} to={ROUTES.QUALITY_RULE_DETAIL(rule.id)} className="grid gap-3 p-4 hover:bg-muted/30 md:grid-cols-[1fr_180px_140px_100px]"><span><strong>{rule.name}</strong><small className="block font-mono text-muted-foreground">{rule.field_path || "entity-level"}</small></span><span>{rule.rule_type}<small className="block text-muted-foreground">{rule.dimension}</small></span><SeverityPill value={rule.severity}/><StatusPill value={rule.is_active ? "active" : "inactive"}/></Link>)}</div><Pagination value={query.data.pagination} onPage={(next) => setParams({ page: String(next) })}/></section> : <EmptyState title="No quality rules" description="Records remain explicitly not evaluated until a steward configures rules." action={<Button onClick={() => navigate(ROUTES.QUALITY_RULE_NEW)}>Create first rule</Button>}/>}</main>; }
