import { useQuery } from "@tanstack/react-query"; import { useParams } from "react-router-dom"; import { MatchingRuleForm } from "../components/MatchingRuleForm"; import { GovernedError, PageSkeleton, QUERY_KEYS } from "../components/MdmUI"; import { masterDataService } from "../services/master-data-service";
export function EditMatchingRulePage() { const { id = "" } = useParams(); const query = useQuery({ queryKey: QUERY_KEYS.matchingRule(id), queryFn: () => masterDataService.matchingRules.get(id), enabled: Boolean(id) }); if (query.isLoading) return <PageSkeleton/>; if (query.error) return <GovernedError error={query.error} retry={() => void query.refetch()}/>; return query.data ? <MatchingRuleForm existing={query.data.data}/> : <GovernedError error={new Error("Matching rule not found.")}/>; }
