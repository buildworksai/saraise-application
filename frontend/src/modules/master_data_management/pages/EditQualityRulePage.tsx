import { useQuery } from "@tanstack/react-query"; import { useParams } from "react-router-dom"; import { QualityRuleForm } from "../components/QualityRuleForm"; import { GovernedError, PageSkeleton, QUERY_KEYS } from "../components/MdmUI"; import { masterDataService } from "../services/master-data-service";
export function EditQualityRulePage() { const { id = "" } = useParams(); const query = useQuery({ queryKey: QUERY_KEYS.qualityRule(id), queryFn: () => masterDataService.qualityRules.get(id), enabled: Boolean(id) }); if (query.isLoading) return <PageSkeleton/>; if (query.error) return <GovernedError error={query.error} retry={() => void query.refetch()}/>; return query.data ? <QualityRuleForm existing={query.data.data}/> : <GovernedError error={new Error("Quality rule not found.")}/>; }
