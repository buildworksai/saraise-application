import { useMutation, useQueryClient } from "@tanstack/react-query"; import { useNavigate } from "react-router-dom"; import type { ToolCreateRequest } from "../contracts"; import { ROUTES } from "../contracts"; import { ToolForm } from "../components/ToolForm"; import { PageHeader } from "../components/AgentUI"; import { aiAgentService } from "../services/ai-agent-service";
export const ToolCreatePage = () => { const navigate = useNavigate(); const client = useQueryClient(); const mutation = useMutation({ mutationFn: (request: ToolCreateRequest) => aiAgentService.createTool(request), onSuccess: (tool) => { void client.invalidateQueries({ queryKey: ["ai-tools"] }); navigate(ROUTES.TOOL_DETAIL(tool.id)); } }); return <main className="mx-auto max-w-5xl space-y-6"><PageHeader title="Register governed tool" description="Publish a closed, schema-validated descriptor for an installed and entitled module implementation."/><ToolForm pending={mutation.isPending} error={mutation.error} submit={(request) => mutation.mutate(request as ToolCreateRequest)}/></main>; };
