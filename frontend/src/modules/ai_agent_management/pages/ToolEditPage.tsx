import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query"; import { useNavigate, useParams } from "react-router-dom"; import type { ToolUpdateRequest } from "../contracts"; import { ROUTES } from "../contracts"; import { ToolForm } from "../components/ToolForm"; import { GovernedError, PageHeader, PageSkeleton } from "../components/AgentUI"; import { aiAgentService } from "../services/ai-agent-service";
export const ToolEditPage = () => { const { id = "" } = useParams(); const navigate = useNavigate(); const client = useQueryClient(); const query = useQuery({ queryKey: ["ai-tool", id], queryFn: () => aiAgentService.getTool(id), enabled: Boolean(id) }); const mutation = useMutation({ mutationFn: (request: ToolUpdateRequest) => aiAgentService.updateTool(id, request), onSuccess: () => { void client.invalidateQueries({ queryKey: ["ai-tool", id] }); navigate(ROUTES.TOOL_DETAIL(id)); } }); if (query.isLoading) return <PageSkeleton/>; if (query.error) return <GovernedError error={query.error} retry={() => void query.refetch()}/>; if (!query.data) return <GovernedError error={new Error("Tool not found.")}/>; return <main className="mx-auto max-w-5xl space-y-6"><PageHeader title={`Edit ${query.data.name}`} description="Update schemas and governance metadata; immutable invocation evidence keeps its recorded descriptor version."/><ToolForm initial={query.data} pending={mutation.isPending} error={mutation.error} submit={(request) => mutation.mutate(request as ToolUpdateRequest)}/></main>; };
