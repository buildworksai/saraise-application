import { useState } from 'react'; import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'; import { Link, useNavigate, useParams } from 'react-router-dom'; import { Edit, Trash2 } from 'lucide-react'; import { toast } from 'sonner'; import { Button } from '@/components/ui/Button'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card';
import { ROUTES, hrKeys } from '../contracts'; import type { DepartmentHierarchyNode } from '../contracts'; import { hrService } from '../services/hr-service'; import { can, ConfirmAction, Detail, DetailGrid, GovernedError, PageShell, PageSkeleton, StatusChip } from '../components/hr-ui';
function Node({ node }: { node: DepartmentHierarchyNode }) { return <li className="ml-4 border-l pl-4"><Link className="font-medium text-primary hover:underline" to={ROUTES.DEPARTMENT_DETAIL(node.id)}>{node.department_name}</Link><span className="ml-2 text-xs text-muted-foreground">{node.department_code}</span>{node.children.length ? <ul className="mt-2 space-y-2">{node.children.map((child) => <Node key={child.id} node={child} />)}</ul> : null}</li>; }
export function DepartmentDetailPage() { const { id = '' } = useParams(); const navigate = useNavigate(); const client = useQueryClient(); const [open, setOpen] = useState(false); const detail = useQuery({ queryKey: hrKeys.department(id), queryFn: () => hrService.getDepartment(id), enabled: Boolean(id) }); const hierarchy = useQuery({ queryKey: hrKeys.hierarchy(id), queryFn: () => hrService.getDepartmentHierarchy(id), enabled: Boolean(id) }); const archive = useMutation({ mutationFn: () => hrService.deleteDepartment(id), onSuccess: () => { void client.invalidateQueries({ queryKey: hrKeys.all }); navigate(ROUTES.DEPARTMENTS); toast.success('Department archived'); } }); if (detail.isLoading) return <PageSkeleton />; if (detail.error || !detail.data) return <PageShell title="Department" description="Department detail."><GovernedError error={detail.error} retry={() => void detail.refetch()} resource="Department" /></PageShell>; const item = detail.data.data; return <PageShell title={item.department_name} description={`${item.department_code} · governed organizational unit`} back={() => navigate(ROUTES.DEPARTMENTS)} actions={<>{can(detail.data.capabilities, 'hr.department:update') ? <Button variant="outline" onClick={() => navigate(ROUTES.DEPARTMENT_EDIT(id))}><Edit className="mr-2 h-4 w-4" />Edit</Button> : null}{can(detail.data.capabilities, 'hr.department:delete') ? <Button variant="danger" onClick={() => setOpen(true)}><Trash2 className="mr-2 h-4 w-4" />Archive</Button> : null}</>}><Card><CardHeader><div className="flex justify-between"><CardTitle>Department profile</CardTitle><StatusChip status={item.is_active ? 'active' : 'inactive'} /></div></CardHeader><CardContent><DetailGrid><Detail label="Parent">{item.parent_department ? <Link className="text-primary hover:underline" to={ROUTES.DEPARTMENT_DETAIL(item.parent_department)}>{item.parent_department_name}</Link> : 'Top level'}</Detail><Detail label="Manager">{item.manager ? <Link className="text-primary hover:underline" to={ROUTES.EMPLOYEE_DETAIL(item.manager)}>{item.manager_name}</Link> : 'Unassigned'}</Detail><Detail label="Description">{item.description || 'No description'}</Detail></DetailGrid></CardContent></Card><Card><CardHeader><CardTitle>Hierarchy branch</CardTitle></CardHeader><CardContent>{hierarchy.isLoading ? <p role="status">Loading hierarchy…</p> : hierarchy.error ? <GovernedError error={hierarchy.error} resource="Department hierarchy" /> : hierarchy.data?.data.length ? <ul>{hierarchy.data.data.map((node) => <Node key={node.id} node={node} />)}</ul> : <p className="text-sm text-muted-foreground">No child departments.</p>}</CardContent></Card><ConfirmAction open={open} onOpenChange={setOpen} title="Archive department?" description="Archiving is blocked while active children or employees remain attached." confirmLabel="Archive department" danger pending={archive.isPending} onConfirm={() => archive.mutate()} />{archive.error ? <GovernedError error={archive.error} resource="Department archive" /> : null}</PageShell>; }
