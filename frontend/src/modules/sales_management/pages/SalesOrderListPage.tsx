import { useQuery } from '@tanstack/react-query';
import { ResourceList, StatusPill, useListFilters } from '../components/SalesUi';
import { SALES_PATHS, type SalesOrderFilters } from '../contracts';
import { salesQueryKeys, salesService } from '../services/sales-service';
export function SalesOrderListPage() { const filters=useListFilters() as SalesOrderFilters; const result=useQuery({queryKey:salesQueryKeys.orders(filters),queryFn:()=>salesService.listOrders(filters)}); return <ResourceList title="Sales orders" description="Track commitments from confirmation through fulfillment and invoicing handoff." createLabel="Create sales order" createPath={`${SALES_PATHS.ORDERS}/new`} detailPath={(id)=>`${SALES_PATHS.ORDERS}/${encodeURIComponent(id)}`} queryResult={result} emptyTitle="No sales orders yet" searchPlaceholder="Search order number" orderingOptions={[{value:'-order_date',label:'Newest first'},{value:'delivery_date',label:'Delivery date'},{value:'-total_amount',label:'Highest value'}]} filterOptions={[{key:'status',label:'Status',options:['draft','confirmed','picking','packing','ready_to_ship','shipped','delivered','invoiced','cancelled'].map((value)=>({value,label:value.replaceAll('_',' ')}))}]} columns={[{key:'number',label:'Order',render:(r)=>r.order_number},{key:'customer',label:'Customer',render:(r)=>r.customer_name??r.customer},{key:'delivery',label:'Delivery',render:(r)=>r.delivery_date??'Not scheduled'},{key:'total',label:'Total',render:(r)=>`${r.currency} ${r.total_amount}`},{key:'status',label:'Status',render:(r)=><StatusPill status={r.status}/>}]} />; }
