import { useQuery } from '@tanstack/react-query';import { useParams } from 'react-router-dom';import { CommercialDocumentForm } from '../components/DocumentForms';import { GovernedError, SalesPage } from '../components/SalesUi';import { salesQueryKeys,salesService } from '../services/sales-service';
export function EditQuotationPage(){const {id}=useParams();const q=useQuery({queryKey:salesQueryKeys.quotation(id??''),queryFn:()=>salesService.getQuotation(id??''),enabled:Boolean(id)});if(q.isLoading)return <SalesPage title="Edit quotation" description="Loading draft…"><div className="h-72 animate-pulse rounded bg-muted"/></SalesPage>;if(q.error||!q.data)return <SalesPage title="Edit quotation" description="Draft unavailable"><GovernedError error={q.error} onRetry={()=>void q.refetch()}/></SalesPage>;return <CommercialDocumentForm kind="quotation" document={q.data}/>;}
