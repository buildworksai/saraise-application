import { useQuery } from '@tanstack/react-query';
import { ResourceList, StatusPill, useListFilters } from '../components/SalesUi';
import { SALES_PATHS, type DeliveryNoteFilters } from '../contracts';
import { salesQueryKeys, salesService } from '../services/sales-service';
export function DeliveryNoteListPage() { const filters=useListFilters() as DeliveryNoteFilters; const result=useQuery({queryKey:salesQueryKeys.deliveries(filters),queryFn:()=>salesService.listDeliveryNotes(filters)}); return <ResourceList title="Deliveries" description="Evidence-backed delivery notes linked to eligible order quantities." createLabel="Create delivery note" createPath={`${SALES_PATHS.DELIVERIES}/new`} detailPath={(id)=>`${SALES_PATHS.DELIVERIES}/${encodeURIComponent(id)}`} queryResult={result} emptyTitle="No delivery notes yet" searchPlaceholder="Search number or tracking" orderingOptions={[{value:'-delivery_date',label:'Newest first'},{value:'delivery_number',label:'Number A–Z'},{value:'created_at',label:'Oldest created'}]} filterOptions={[{key:'status',label:'Status',options:['draft','completed','cancelled'].map((value)=>({value,label:value}))}]} columns={[{key:'number',label:'Delivery',render:(r)=>r.delivery_number},{key:'order',label:'Order',render:(r)=>r.order_number??r.sales_order},{key:'date',label:'Date',render:(r)=>r.delivery_date},{key:'tracking',label:'Tracking',render:(r)=>r.tracking_number||'—'},{key:'status',label:'Status',render:(r)=><StatusPill status={r.status}/>}]} />; }
