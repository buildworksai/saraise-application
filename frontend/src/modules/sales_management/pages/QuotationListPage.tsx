import { useQuery } from '@tanstack/react-query';
import { ResourceList, StatusPill, useListFilters } from '../components/SalesUi';
import { SALES_PATHS, type QuotationFilters } from '../contracts';
import { salesQueryKeys, salesService } from '../services/sales-service';
export function QuotationListPage() { const filters=useListFilters() as QuotationFilters; const result=useQuery({queryKey:salesQueryKeys.quotations(filters),queryFn:()=>salesService.listQuotations(filters)}); return <ResourceList title="Quotations" description="Versioned, auditable offers with server-calculated totals and controlled transitions." createLabel="Create quotation" createPath={`${SALES_PATHS.QUOTATIONS}/new`} detailPath={(id)=>`${SALES_PATHS.QUOTATIONS}/${encodeURIComponent(id)}`} queryResult={result} emptyTitle="No quotations yet" searchPlaceholder="Search quotation number" orderingOptions={[{value:'-quotation_date',label:'Newest first'},{value:'valid_until',label:'Expiring first'},{value:'-total_amount',label:'Highest value'}]} filterOptions={[{key:'status',label:'Status',options:['draft','sent','accepted','rejected','expired','converted'].map((value)=>({value,label:value.replaceAll('_',' ')}))}]} columns={[{key:'number',label:'Quotation',render:(r)=>r.quotation_number},{key:'customer',label:'Customer',render:(r)=>r.customer_name??r.customer},{key:'valid',label:'Valid until',render:(r)=>r.valid_until},{key:'total',label:'Total',render:(r)=>`${r.currency} ${r.total_amount}`},{key:'status',label:'Status',render:(r)=><StatusPill status={r.status}/>}]} />; }
