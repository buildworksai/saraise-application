import { useMutation } from "@tanstack/react-query"; import { useState } from "react"; import { useNavigate } from "react-router-dom"; import { Button } from "@/components/ui/Button"; import { Input } from "@/components/ui/Input"; import { Textarea } from "@/components/ui/Textarea"; import { GovernedError,PageHeader,Surface } from "../components/CustomizationUI"; import { TargetContractSelector } from "../components/TargetContractSelector"; import { useUnsavedChanges } from "../components/useUnsavedChanges"; import { ROUTES,type RuleCreateRequest,type RuleTrigger } from "../contracts"; import { customizationFrameworkService as service } from "../services/customization-framework-service";
export function CreateRulePage(){const navigate=useNavigate();const[name,setName]=useState("");const[key,setKey]=useState("");const[description,setDescription]=useState("");const[module,setModule]=useState("");const[resource,setResource]=useState("");const[version,setVersion]=useState("1.0");const[trigger,setTrigger]=useState<RuleTrigger>("validate");const[priority,setPriority]=useState("100");const[error,setError]=useState("");useUnsavedChanges(Boolean(name||key||description));const mutation=useMutation({mutationFn:(request:RuleCreateRequest)=>service.createRule(request),onSuccess:result=>navigate(ROUTES.RULE_EDIT(result.data.id))});function submit(event:React.FormEvent){event.preventDefault();const numeric=Number(priority);if(!name.trim()||!/^[a-z0-9]+(?:-[a-z0-9]+)*$/u.test(key)||!module||!resource||!Number.isInteger(numeric)||numeric<1||numeric>1000){setError("Complete all fields. Priority must be an integer from 1 to 1000.");return}setError("");mutation.mutate({key,name,description,owner_module:module,target_resource:resource,target_contract_version:version,trigger,priority:numeric})}return <main className="space-y-6"><PageHeader title="Create business rule" description="Define rule identity and deterministic order, then compose a candidate version with allowlisted operators and actions."/><form onSubmit={submit} className="space-y-6"><Surface><div className="grid gap-5 sm:grid-cols-2"><TargetContractSelector value={module?`${module}/${resource}@${version}`:""} onSelect={contract=>{setModule(contract.module);setResource(contract.resource);setVersion(contract.version);if(!contract.rule_triggers.includes(trigger))setTrigger(contract.rule_triggers[0]??"validate")}}/><Input id="rule-name" label="Name" required value={name} onChange={event=>{setName(event.target.value);if(!key)setKey(event.target.value.toLowerCase().replace(/[^a-z0-9]+/gu,"-").replace(/^-|-$/gu,""))}}/><Input id="rule-key" label="Stable key" required value={key} onChange={event=>setKey(event.target.value)}/><label className="grid gap-1 text-sm font-medium">Trigger<select className="h-10 rounded-md border bg-background px-3" value={trigger} onChange={event=>setTrigger(event.target.value as RuleTrigger)}>{["validate","before_create","before_update","form_change"].map(value=><option key={value}>{value}</option>)}</select></label><Input id="rule-priority" label="Priority" type="number" min="1" max="1000" value={priority} onChange={event=>setPriority(event.target.value)}/></div><Textarea id="rule-description" className="mt-5" aria-label="Description" value={description} onChange={event=>setDescription(event.target.value)}/>{error?<p role="alert" className="mt-4 text-sm text-destructive">{error}</p>:null}</Surface>{mutation.error?<GovernedError error={mutation.error}/>:null}<div className="flex justify-end gap-3"><Button type="button" variant="outline" onClick={()=>navigate(ROUTES.RULES)}>Cancel</Button><Button type="submit" disabled={mutation.isPending}>{mutation.isPending?"Creating…":"Create and build"}</Button></div></form></main>}
