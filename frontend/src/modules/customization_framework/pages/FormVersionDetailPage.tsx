import { useQuery } from "@tanstack/react-query"; import { useParams } from "react-router-dom"; import { Detail, DetailGrid, GovernedError, PageHeader, PageSkeleton, StatusChip, Surface } from "../components/CustomizationUI"; import { formatDate } from "../components/customization-utils"; import { customizationFrameworkService as service } from "../services/customization-framework-service";
export function FormVersionDetailPage(){const{versionId=""}=useParams();const query=useQuery({queryKey:["customization","layout",versionId],queryFn:()=>service.getFormLayout(versionId),enabled:Boolean(versionId)});if(query.isLoading)return <PageSkeleton/>;if(query.error)return <GovernedError error={query.error} retry={()=>void query.refetch()}/>;if(!query.data)return <GovernedError error={new Error("Layout version not found.")}/>;const item=query.data.data;return <main className="space-y-6"><PageHeader title={`Layout version ${item.version}`} description="Immutable, content-addressed publication evidence."/><Surface><DetailGrid><Detail label="Status"><StatusChip status={item.status}/></Detail><Detail label="Created">{formatDate(item.created_at)}</Detail><Detail label="Content hash"><span className="font-mono text-xs">{item.content_hash}</span></Detail><Detail label="Schema version">{item.schema_version}</Detail><Detail label="Summary">{item.change_summary}</Detail><Detail label="Published">{formatDate(item.published_at)}</Detail></DetailGrid></Surface><Surface><h2 className="font-semibold">Validated layout</h2><pre className="mt-4 max-h-[34rem] overflow-auto rounded-lg bg-muted p-4 text-xs">{JSON.stringify(item.layout,null,2)}</pre>{item.validation_errors.length?<ul className="mt-4 text-sm text-destructive">{item.validation_errors.map(d=><li key={`${d.code}-${d.pointer}`}>{d.message}</li>)}</ul>:<p className="mt-3 text-sm text-emerald-700">No validation diagnostics.</p>}</Surface></main>}
