import { useQuery } from "@tanstack/react-query"; import { useParams } from "react-router-dom"; import { Detail,DetailGrid,GovernedError,PageHeader,PageSkeleton,StatusChip,Surface } from "../components/CustomizationUI"; import { formatDate } from "../components/customization-utils"; import { customizationFrameworkService as service } from "../services/customization-framework-service";
export function RuleVersionDetailPage(){const{versionId=""}=useParams();const query=useQuery({queryKey:["customization","rule-version",versionId],queryFn:()=>service.getRuleVersion(versionId),enabled:Boolean(versionId)});if(query.isLoading)return <PageSkeleton/>;if(query.error)return <GovernedError error={query.error} retry={()=>void query.refetch()}/>;if(!query.data)return <GovernedError error={new Error("Rule version not found.")}/>;const item=query.data.data;return <main className="space-y-6"><PageHeader title={`Rule version ${item.version}`} description="Immutable deterministic AST and validation evidence."/><Surface><DetailGrid><Detail label="Status"><StatusChip status={item.status}/></Detail><Detail label="Language version">{item.language_version}</Detail><Detail label="Created">{formatDate(item.created_at)}</Detail><Detail label="Hash"><span className="font-mono text-xs">{item.content_hash}</span></Detail><Detail label="Dependencies">{item.dependencies.length}</Detail><Detail label="Summary">{item.change_summary}</Detail></DetailGrid></Surface><div className="grid gap-6 lg:grid-cols-2"><Surface><h2 className="font-semibold">Condition</h2><pre className="mt-3 overflow-auto rounded bg-muted p-3 text-xs">{JSON.stringify(item.condition_ast,null,2)}</pre></Surface><Surface><h2 className="font-semibold">Returned actions</h2><pre className="mt-3 overflow-auto rounded bg-muted p-3 text-xs">{JSON.stringify(item.action_ast,null,2)}</pre></Surface></div>{item.validation_errors.length?<Surface><h2 className="font-semibold text-destructive">Diagnostics</h2><ul className="mt-3">{item.validation_errors.map(d=><li key={`${d.code}-${d.pointer}`}>{d.message}</li>)}</ul></Surface>:null}</main>}
