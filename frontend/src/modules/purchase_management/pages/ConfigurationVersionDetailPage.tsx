import { useEffect } from 'react'; import { useMutation, useQuery } from '@tanstack/react-query'; import { Link, useParams } from 'react-router-dom'; import { Button } from '@/components/ui/Button'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card'; import { ROUTES } from '../contracts'; import { purchaseService } from '../services/purchase-service';
export const ConfigurationVersionDetailPage = () => { useEffect(() => { document.title = 'Configuration version | SARAISE'; }, []); const { id = '' } = useParams(); const query = useQuery({ queryKey: ['purchase-config-version', id], queryFn: () => purchaseService.getConfigurationVersion(id) }); const activate = useMutation({ mutationFn: () => purchaseService.activateConfiguration(id, 'Reviewed and approved in configuration console', { idempotencyKey: crypto.randomUUID(), lockVersion: query.data?.lock_version }) }); const rollback = useMutation({ mutationFn: () => purchaseService.rollbackConfiguration(id, 'Operator rollback to selected evidence version', { idempotencyKey: crypto.randomUUID() }) }); if (query.isLoading) return <div className="h-72 animate-pulse bg-muted p-8" />; if (!query.data) return <main className="p-8"><h1>Configuration version not found</h1></main>; return <main className="space-y-6 p-8"><Link className="text-primary" to={ROUTES.SETTINGS}>Back to settings</Link><Card><CardHeader><CardTitle>Version {query.data.version} · {query.data.status}</CardTitle></CardHeader><CardContent className="space-y-4"><pre className="overflow-auto rounded-md bg-muted p-4 text-xs">{JSON.stringify(query.data, null, 2)}</pre><div className="flex gap-2">{query.data.status === 'draft' && <Button disabled={activate.isPending} onClick={() => activate.mutate()}>Activate after review</Button>}<Button variant="outline" disabled={rollback.isPending} onClick={() => rollback.mutate()}>Create rollback version</Button></div><p className="text-sm text-muted-foreground">Activation archives the prior active version atomically and records actor, reason, versions, and correlation evidence.</p></CardContent></Card></main>; };
