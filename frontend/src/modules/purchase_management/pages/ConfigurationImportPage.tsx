import { useEffect, useState } from 'react'; import { useMutation } from '@tanstack/react-query'; import { Link, useNavigate } from 'react-router-dom'; import { Button } from '@/components/ui/Button'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card'; import type { ConfigurationDocument } from '../contracts'; import { ROUTES } from '../contracts'; import { purchaseService } from '../services/purchase-service';
export const ConfigurationImportPage = () => { useEffect(() => { document.title = 'Import procurement configuration | SARAISE'; }, []); const navigate = useNavigate(); const [value, setValue] = useState(''); const [error, setError] = useState(''); const mutation = useMutation({ mutationFn: (document: ConfigurationDocument) => purchaseService.importConfiguration(document, { idempotencyKey: crypto.randomUUID() }), onSuccess: version => navigate(ROUTES.CONFIGURATION_VERSION(version.id)) }); const submit = () => { try { const document = JSON.parse(value) as ConfigurationDocument; if (document.schema !== 'saraise.purchase.configuration.v1' || !document.checksum) throw new Error('A supported schema and checksum are required.'); setError(''); mutation.mutate(document); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Invalid document'); } }; return <main className="space-y-6 p-8"><Link className="text-primary" to={ROUTES.SETTINGS}>Back</Link><Card><CardHeader><CardTitle>Import a checksummed configuration</CardTitle></CardHeader><CardContent className="space-y-4"><textarea aria-label="Configuration import document" className="min-h-[30rem] w-full rounded-md border border-input bg-background p-4 font-mono text-sm" value={value} onChange={event => setValue(event.target.value)} /><p className="text-sm text-muted-foreground">Import validates schema, bounds and checksum and creates a draft only. It never activates values or imports secrets.</p>{(error || mutation.error) && <p role="alert" className="text-destructive">{error || mutation.error?.message}</p>}<Button disabled={mutation.isPending} onClick={submit}>Validate and create draft</Button></CardContent></Card></main>; };
