import type{ApiPage,ApiSuccess,ArchiveRequest,ListFilters,TimeEntry,TimeEntryCreateRequest,TimeEntryUpdateRequest}from'../contracts';import{ENDPOINTS}from'../contracts';import{apiClient,archive,detail,idempotencyHeaders,page,withQuery}from'./client';
export const timeEntryService={list:(f:ListFilters={})=>page(apiClient.get<ApiPage<TimeEntry>>(withQuery(ENDPOINTS.TIME_ENTRIES.LIST,f))),get:(id:string)=>detail(apiClient.get<ApiSuccess<TimeEntry>>(ENDPOINTS.TIME_ENTRIES.DETAIL(id))),create:(d:TimeEntryCreateRequest,k:string)=>detail(apiClient.post<ApiSuccess<TimeEntry>>(ENDPOINTS.TIME_ENTRIES.CREATE,d,idempotencyHeaders(k))),update:(id:string,d:TimeEntryUpdateRequest)=>detail(apiClient.patch<ApiSuccess<TimeEntry>>(ENDPOINTS.TIME_ENTRIES.UPDATE(id),d,idempotencyHeaders(d.idempotency_key))),archive:(id:string,d:ArchiveRequest)=>archive(ENDPOINTS.TIME_ENTRIES.ARCHIVE(id),d.version,d.idempotency_key),restore:(id:string,d:ArchiveRequest)=>detail(apiClient.post<ApiSuccess<TimeEntry>>(ENDPOINTS.TIME_ENTRIES.RESTORE(id),d,idempotencyHeaders(d.idempotency_key)))};
