import type {ApiPage,ApiSuccess,ArchiveRequest,ListFilters,Task,TaskCreateRequest,TaskUpdateRequest,TransitionRequest} from '../contracts';import{ENDPOINTS}from'../contracts';import{apiClient,archive,detail,idempotencyHeaders,page,withQuery}from'./client';
export const taskService={list:(f:ListFilters={})=>page(apiClient.get<ApiPage<Task>>(withQuery(ENDPOINTS.TASKS.LIST,f))),get:(id:string)=>detail(apiClient.get<ApiSuccess<Task>>(ENDPOINTS.TASKS.DETAIL(id))),create:(d:TaskCreateRequest,k:string)=>detail(apiClient.post<ApiSuccess<Task>>(ENDPOINTS.TASKS.CREATE,d,idempotencyHeaders(k))),update:(id:string,d:TaskUpdateRequest)=>detail(apiClient.patch<ApiSuccess<Task>>(ENDPOINTS.TASKS.UPDATE(id),d,idempotencyHeaders(d.idempotency_key))),archive:(id:string,d:ArchiveRequest)=>archive(ENDPOINTS.TASKS.ARCHIVE(id),d.version,d.idempotency_key),restore:(id:string,d:ArchiveRequest)=>detail(apiClient.post<ApiSuccess<Task>>(ENDPOINTS.TASKS.RESTORE(id),d,idempotencyHeaders(d.idempotency_key))),transition:(id:string,path:string,d:TransitionRequest)=>detail(apiClient.post<ApiSuccess<Task>>(`${ENDPOINTS.TASKS.DETAIL(id)}${path}/`,d,idempotencyHeaders(d.transition_key)))};
