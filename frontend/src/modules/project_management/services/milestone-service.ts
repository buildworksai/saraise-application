import type{ApiPage,ApiSuccess,ArchiveRequest,ListFilters,MilestoneCreateRequest,MilestoneUpdateRequest,ProjectMilestone}from'../contracts';import{ENDPOINTS}from'../contracts';import{apiClient,archive,detail,idempotencyHeaders,page,withQuery}from'./client';
export const milestoneService={list:(f:ListFilters={})=>page(apiClient.get<ApiPage<ProjectMilestone>>(withQuery(ENDPOINTS.MILESTONES.LIST,f))),get:(id:string)=>detail(apiClient.get<ApiSuccess<ProjectMilestone>>(ENDPOINTS.MILESTONES.DETAIL(id))),create:(d:MilestoneCreateRequest,k:string)=>detail(apiClient.post<ApiSuccess<ProjectMilestone>>(ENDPOINTS.MILESTONES.CREATE,d,idempotencyHeaders(k))),update:(id:string,d:MilestoneUpdateRequest)=>detail(apiClient.patch<ApiSuccess<ProjectMilestone>>(ENDPOINTS.MILESTONES.UPDATE(id),d,idempotencyHeaders(d.idempotency_key))),archive:(id:string,d:ArchiveRequest)=>archive(ENDPOINTS.MILESTONES.ARCHIVE(id),d.version,d.idempotency_key),restore:(id:string,d:ArchiveRequest)=>detail(apiClient.post<ApiSuccess<ProjectMilestone>>(ENDPOINTS.MILESTONES.RESTORE(id),d,idempotencyHeaders(d.idempotency_key))),achieve:(id:string,achieved_date:string,key:string)=>detail(apiClient.post<ApiSuccess<ProjectMilestone>>(ENDPOINTS.MILESTONES.ACHIEVE(id),{achieved_date,idempotency_key:key},idempotencyHeaders(key)))};
