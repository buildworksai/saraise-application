import type{ApiPage,ApiSuccess,ListFilters,MemberCreateRequest,MemberUpdateRequest,ProjectMember}from'../contracts';import{ENDPOINTS}from'../contracts';import{apiClient,archive,detail,idempotencyHeaders,page,withQuery}from'./client';
export const memberService={list:(f:ListFilters={})=>page(apiClient.get<ApiPage<ProjectMember>>(withQuery(ENDPOINTS.MEMBERS.LIST,f))),get:(id:string)=>detail(apiClient.get<ApiSuccess<ProjectMember>>(ENDPOINTS.MEMBERS.DETAIL(id))),create:(d:MemberCreateRequest,k:string)=>detail(apiClient.post<ApiSuccess<ProjectMember>>(ENDPOINTS.MEMBERS.CREATE,d,idempotencyHeaders(k))),update:(id:string,d:MemberUpdateRequest)=>detail(apiClient.patch<ApiSuccess<ProjectMember>>(ENDPOINTS.MEMBERS.UPDATE(id),d,idempotencyHeaders(d.idempotency_key))),archive:(id:string,k:string)=>archive(ENDPOINTS.MEMBERS.ARCHIVE(id),1,k),restore:(id:string,k:string)=>detail(apiClient.post<ApiSuccess<ProjectMember>>(ENDPOINTS.MEMBERS.RESTORE(id),{idempotency_key:k},idempotencyHeaders(k)))};
