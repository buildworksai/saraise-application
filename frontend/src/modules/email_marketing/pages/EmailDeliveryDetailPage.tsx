import { useQuery } from '@tanstack/react-query'; import { Link, useParams } from 'react-router-dom'; import { Detail, DetailGrid, formatDate, GovernedError, Page, PageSkeleton, Status, Surface } from '../components/EmailMarketingUI'; import { ROUTES } from '../contracts'; import { EMAIL_MARKETING_QUERY_KEYS, emailMarketingService } from '../services/email-marketing-service';
export function EmailDeliveryDetailPage() { const { id = '' } = useParams(); const query = useQuery({ queryKey: EMAIL_MARKETING_QUERY_KEYS.delivery(id), queryFn: () => emailMarketingService.deliveries.get(id), enabled: Boolean(id) }); if (query.isLoading) return <PageSkeleton label="Loading delivery evidence"/>; if (query.error) return <Page title="Delivery attempt" description="Gateway acknowledgement and sanitized evidence."><GovernedError error={query.error} retry={() => void query.refetch()}/></Page>; if (!query.data) return null; const item = query.data.data; return <Page title={`Delivery attempt ${item.attempt_number}`} description="Accepted means a real gateway acknowledgement; delivered requires provider evidence." back={{ label: 'Audience & delivery', to: `${ROUTES.DELIVERY}?view=deliveries` }}><Surface title="Gateway evidence"><DetailGrid><Detail label="Status"><Status value={item.status}/></Detail><Detail label="Recipient"><Link className="text-primary hover:underline" to={ROUTES.RECIPIENT_DETAIL(item.recipient_id)}>{item.recipient_id}</Link></Detail><Detail label="Gateway">{item.gateway_key}</Detail><Detail label="Provider message">{item.provider_message_id || 'No acknowledgement ID'}</Detail><Detail label="Provider status">{item.provider_status_code || '—'}</Detail><Detail label="Job">{item.job_id}</Detail><Detail label="Started">{formatDate(item.started_at)}</Detail><Detail label="Accepted">{formatDate(item.accepted_at)}</Detail><Detail label="Completed">{formatDate(item.completed_at)}</Detail></DetailGrid>{item.error_code ? <div className="mt-5 rounded-lg border border-destructive/30 p-3 text-sm"><strong>{item.error_code}</strong><p>{item.error_detail}</p></div> : null}</Surface><Surface title="Allowlisted response evidence"><pre className="overflow-auto whitespace-pre-wrap rounded bg-muted p-4 text-xs">{JSON.stringify(item.response_evidence, null, 2)}</pre></Surface><Surface title="Verified events">{item.events.length ? <ol className="space-y-3">{item.events.map((event) => <li key={event.id} className="rounded border p-3 text-sm"><Status value={event.event_type}/><span className="ml-2">{formatDate(event.occurred_at)}</span><p className="mt-1 font-mono text-xs">{event.gateway_key} · {event.provider_event_id} · correlation {event.correlation_id}</p></li>)}</ol> : <p className="text-sm text-muted-foreground">No verified provider event has been appended.</p>}</Surface></Page>; }
