import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { useNavigate, useParams } from 'react-router-dom';
import { CampaignForm } from '../components/CampaignForm';
import { GovernedError, Page, PageSkeleton, Surface } from '../components/EmailMarketingUI';
import { ROUTES } from '../contracts';
import { EMAIL_MARKETING_QUERY_KEYS, emailMarketingService } from '../services/email-marketing-service';
export function EditEmailCampaignPage() { const { id = '' } = useParams(); const navigate = useNavigate(); const client = useQueryClient(); const query = useQuery({ queryKey: EMAIL_MARKETING_QUERY_KEYS.campaign(id), queryFn: () => emailMarketingService.campaigns.get(id), enabled: Boolean(id) }); const mutation = useMutation({ mutationFn: (input: Parameters<typeof emailMarketingService.campaigns.update>[1]) => emailMarketingService.campaigns.update(id, input), onSuccess: async (result) => { await client.invalidateQueries({ queryKey: EMAIL_MARKETING_QUERY_KEYS.all }); navigate(ROUTES.CAMPAIGN_DETAIL(result.data.id)); } }); if (query.isLoading) return <PageSkeleton label="Loading campaign editor"/>; if (query.error) return <Page title="Edit campaign" description="Only drafts can be edited." back={{ label: 'Campaigns', to: ROUTES.CAMPAIGNS }}><GovernedError error={query.error} retry={() => void query.refetch()}/></Page>; if (!query.data) return null; if (query.data.data.status !== 'draft') return <Page title="Campaign is immutable" description="Lifecycle changes use explicit, auditable actions." back={{ label: 'Campaign detail', to: ROUTES.CAMPAIGN_DETAIL(id) }}><Surface>This campaign is <strong>{query.data.data.status}</strong>. Return a scheduled campaign to draft before editing; queued, sent, or terminal campaign content cannot be rewritten.</Surface></Page>; return <Page title={`Edit ${query.data.data.campaign_name}`} description="Draft-only authoring preserves the evidence snapshot of every queued send." back={{ label: 'Campaign detail', to: ROUTES.CAMPAIGN_DETAIL(id) }}><CampaignForm key={query.data.data.updated_at} initial={query.data.data} pending={mutation.isPending} serverError={mutation.error} onSubmit={(input) => mutation.mutate(input)}/></Page>; }
