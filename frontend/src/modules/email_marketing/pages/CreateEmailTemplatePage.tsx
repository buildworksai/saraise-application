import { useMutation, useQueryClient } from '@tanstack/react-query';
import { useNavigate } from 'react-router-dom';
import { TemplateForm } from '../components/TemplateForm'; import { Page } from '../components/EmailMarketingUI'; import { ROUTES } from '../contracts'; import { EMAIL_MARKETING_QUERY_KEYS, emailMarketingService } from '../services/email-marketing-service';
export function CreateEmailTemplatePage() { const navigate = useNavigate(); const client = useQueryClient(); const mutation = useMutation({ mutationFn: emailMarketingService.templates.create, onSuccess: async (result) => { await client.invalidateQueries({ queryKey: EMAIL_MARKETING_QUERY_KEYS.all }); navigate(ROUTES.TEMPLATE_DETAIL(result.data.id)); } }); return <Page title="Create email template" description="Sanitized HTML, a plain-text alternative, and versioned design data create portable campaign content." back={{ label: 'Templates', to: ROUTES.TEMPLATES }}><TemplateForm pending={mutation.isPending} serverError={mutation.error} onSubmit={(input) => mutation.mutate(input)}/></Page>; }
