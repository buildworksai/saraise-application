/* eslint-disable complexity -- dual ledgers intentionally share URL-backed operational context. */
import { useQuery } from '@tanstack/react-query';
import { Link, useSearchParams } from 'react-router-dom';
import { Button } from '@/components/ui/Button';
import { EmptyPanel, GovernedError, Page, PageSkeleton, Pagination, RefreshNotice, Status } from '../components/EmailMarketingUI';
import { ROUTES, type DeliveryAttemptStatus, type RecipientStatus } from '../contracts';
import { EMAIL_MARKETING_QUERY_KEYS, emailMarketingService } from '../services/email-marketing-service';
import { configuredPageSize, useEmailMarketingConfiguration } from '../hooks/use-email-marketing-configuration';
const set = (params: URLSearchParams, update: ReturnType<typeof useSearchParams>[1], key: string, value: string) => { const next = new URLSearchParams(params); if (value) next.set(key, value); else next.delete(key); if (key !== 'page') next.delete('page'); update(next); };
export function AudienceDeliveryListPage() { const [params, update] = useSearchParams(); const configuration = useEmailMarketingConfiguration(); const document = configuration.data?.data.document; const view = params.get('view') === 'deliveries' ? 'deliveries' : 'recipients'; const campaign = params.get('campaign_id') ?? ''; const status = params.get('status') ?? ''; const email = params.get('email') ?? ''; const gateway = params.get('gateway_key') ?? ''; const ordering = params.get('ordering') ?? (view === 'recipients' ? document?.filters.default_ordering_by_resource.recipients : document?.filters.default_ordering_by_resource.deliveries) ?? ''; const page = Math.max(1, Number(params.get('page') ?? 1)); const pageSize = document ? configuredPageSize(params.get('page_size'), document) : Number.NaN; const recipientFilters = { campaign_id: campaign || undefined, status: status ? status as RecipientStatus : undefined, email: email || undefined, ordering: ordering as '-created_at', page, page_size: pageSize }; const deliveryFilters = { campaign_id: campaign || undefined, status: status ? status as DeliveryAttemptStatus : undefined, gateway_key: gateway || undefined, ordering: ordering as '-created_at', page, page_size: pageSize }; const recipients = useQuery({ queryKey: EMAIL_MARKETING_QUERY_KEYS.recipients(recipientFilters), queryFn: () => emailMarketingService.recipients.list(recipientFilters), enabled: Boolean(document) && view === 'recipients' }); const deliveries = useQuery({ queryKey: EMAIL_MARKETING_QUERY_KEYS.deliveries(deliveryFilters), queryFn: () => emailMarketingService.deliveries.list(deliveryFilters), enabled: Boolean(document) && view === 'deliveries' }); const query = view === 'recipients' ? recipients : deliveries; const reset = () => update(new URLSearchParams({ view })); const filtered = Boolean(campaign || status || email || gateway); if (configuration.isLoading) return <PageSkeleton label="Loading delivery configuration"/>; if (configuration.error) return <Page title="Audience & delivery" description="Recipient truth, durable attempts, and provider evidence."><GovernedError error={configuration.error} retry={() => void configuration.refetch()}/></Page>; if (!document) return <Page title="Audience & delivery" description="Recipient truth, durable attempts, and provider evidence."><GovernedError error={new Error('No governed configuration response was received.')} retry={() => void configuration.refetch()}/></Page>; if (query.isLoading) return <PageSkeleton label="Loading audience and delivery evidence"/>; if (query.error) return <Page title="Audience & delivery" description="Recipient truth, durable attempts, and provider evidence."><GovernedError error={query.error} retry={() => void query.refetch()}/></Page>; if (!query.data) return <Page title="Audience & delivery" description="Recipient truth, durable attempts, and provider evidence."><GovernedError error={new Error('No governed delivery response was received.')} retry={() => void query.refetch()}/></Page>; return <Page title="Audience & delivery" description="Inspect consent-aware recipient decisions separately from real gateway attempts."><div role="tablist" aria-label="Delivery views" className="flex gap-2"><Button role="tab" aria-selected={view === 'recipients'} variant={view === 'recipients' ? 'primary' : 'outline'} onClick={() => update(new URLSearchParams({ view: 'recipients' }))}>Recipients</Button><Button role="tab" aria-selected={view === 'deliveries'} variant={view === 'deliveries' ? 'primary' : 'outline'} onClick={() => update(new URLSearchParams({ view: 'deliveries' }))}>Delivery attempts</Button></div><section aria-label="Delivery filters" className="grid gap-3 rounded-xl border bg-card p-4 lg:grid-cols-6"><input aria-label="Filter campaign ID" className="rounded-md border bg-background p-2" placeholder="Campaign UUID" value={campaign} onChange={(event) => set(params, update, 'campaign_id', event.target.value)}/><input aria-label="Filter status" maxLength={document.limits.search_max_length} className="rounded-md border bg-background p-2" placeholder="Exact status" value={status} onChange={(event) => set(params, update, 'status', event.target.value)}/>{view === 'recipients' ? <input aria-label="Filter exact email" type="email" className="rounded-md border bg-background p-2" placeholder="Exact email" value={email} onChange={(event) => set(params, update, 'email', event.target.value)}/> : <select aria-label="Filter gateway" className="rounded-md border bg-background p-2" value={gateway} onChange={(event) => set(params, update, 'gateway_key', event.target.value)}><option value="">Any gateway</option>{document.integrations.gateway_keys.map((value) => <option key={value}>{value}</option>)}</select>}<select aria-label="Order evidence" className="rounded-md border bg-background p-2" value={ordering} onChange={(event) => set(params, update, 'ordering', event.target.value)}><option value="-created_at">Newest first</option><option value="created_at">Oldest first</option><option value="status">Status</option></select><select aria-label="Delivery page size" className="rounded-md border bg-background p-2" value={pageSize} onChange={(event) => set(params, update, 'page_size', event.target.value)}>{document.pagination.page_size_options.map((value) => <option key={value}>{value} / page</option>)}</select><Button variant="outline" onClick={reset}>Reset</Button></section><RefreshNotice active={query.isFetching}/>{view === 'recipients' ? <RecipientResults query={recipients} filtered={filtered} reset={reset} page={(next) => set(params, update, 'page', String(next))}/> : <DeliveryResults query={deliveries} filtered={filtered} reset={reset} page={(next) => set(params, update, 'page', String(next))}/>}</Page>; }
function RecipientResults({ query, filtered, reset, page }: { readonly query: ReturnType<typeof useQuery<Awaited<ReturnType<typeof emailMarketingService.recipients.list>>>>; readonly filtered: boolean; readonly reset: () => void; readonly page: (next: number) => void }) { if (!query.data) return <GovernedError error={new Error('No governed recipient response was received.')} retry={() => void query.refetch()}/>; return query.data.items.length === 0 ? <EmptyPanel filtered={filtered} noun="recipients" reset={reset}/> : <section className="overflow-hidden rounded-xl border bg-card"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-sm"><thead className="bg-muted text-left"><tr><th className="p-3">Recipient</th><th className="p-3">Campaign</th><th className="p-3">Status</th><th className="p-3">Resolved</th></tr></thead><tbody className="divide-y">{query.data.items.map((item) => <tr key={item.id}><td className="p-3"><Link className="font-medium text-primary hover:underline" to={ROUTES.RECIPIENT_DETAIL(item.id)}>{item.display_name || item.email}</Link><small className="block text-muted-foreground">{item.email}</small></td><td className="p-3 font-mono text-xs">{item.campaign_id}</td><td className="p-3"><Status value={item.status}/>{item.suppression_reason ? <small className="ml-2">{item.suppression_reason}</small> : null}</td><td className="p-3">{new Date(item.created_at).toLocaleString()}</td></tr>)}</tbody></table></div><Pagination value={query.data.pagination} onPage={page}/></section>; }
function DeliveryResults({ query, filtered, reset, page }: { readonly query: ReturnType<typeof useQuery<Awaited<ReturnType<typeof emailMarketingService.deliveries.list>>>>; readonly filtered: boolean; readonly reset: () => void; readonly page: (next: number) => void }) { if (!query.data) return <GovernedError error={new Error('No governed delivery response was received.')} retry={() => void query.refetch()}/>; return query.data.items.length === 0 ? <EmptyPanel filtered={filtered} noun="delivery attempts" reset={reset}/> : <section className="overflow-hidden rounded-xl border bg-card"><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-sm"><thead className="bg-muted text-left"><tr><th className="p-3">Attempt</th><th className="p-3">Recipient</th><th className="p-3">Status</th><th className="p-3">Evidence time</th></tr></thead><tbody className="divide-y">{query.data.items.map((item) => <tr key={item.id}><td className="p-3"><Link className="font-medium text-primary hover:underline" to={ROUTES.DELIVERY_DETAIL(item.id)}>Attempt {item.attempt_number}</Link><small className="block font-mono text-muted-foreground">{item.id}</small></td><td className="p-3 font-mono text-xs">{item.recipient_id}</td><td className="p-3"><Status value={item.status}/>{item.error_code ? <small className="ml-2 text-destructive">{item.error_code}</small> : null}</td><td className="p-3">{new Date(item.completed_at ?? item.created_at).toLocaleString()}</td></tr>)}</tbody></table></div><Pagination value={query.data.pagination} onPage={page}/></section>; }
