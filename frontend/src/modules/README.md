# frontend/src/modules/

Purpose
- Frontend module feature areas (UI + routing + module-specific services).

Conventions
- Organize per module: `frontend/src/modules/{module_name}/...`.
- Keep shared UI in shared component areas (as they are introduced), not duplicated across modules.
