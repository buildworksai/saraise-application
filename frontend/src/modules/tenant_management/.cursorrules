# Tenant Management Module - Runtime Plane Rules

# This module is strictly read-only. Tenant lifecycle, module enablement,
# quotas, and settings mutation belong to the Control Plane.

# Read contracts.ts first and use its exported types and ENDPOINTS.
# Only apiClient.get is permitted in this module.
# POST, PUT, PATCH, DELETE, and useMutation are forbidden.

# Validate every edit with TypeScript and ESLint at zero errors/warnings.
