# frontend/src/

Purpose
- Frontend application source.

Conventions
- TypeScript strictness is mandatory (no implicit any, no unsafe any).
- Keep API access in `frontend/src/services/`.
- Module UIs live in `frontend/src/modules/`.
