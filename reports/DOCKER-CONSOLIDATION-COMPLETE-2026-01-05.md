# Docker Consolidation Complete — Single Compose File

**Date:** January 5, 2026  
**Status:** ✅ COMPLETE

---

## Summary

Created a consolidated `docker-compose.dev.yml` that includes all SARAISE services (Phase 1-6) in a single file with unified network and port standardization.

---

## Consolidated Services

### Infrastructure (2 services)
- **PostgreSQL:** `saraise-db` - Port `15432:5432`
- **Redis:** `saraise-redis` - Port `16379:6379`

### Phase 2 Services (4 services)
- **Auth Service:** `saraise-auth` - Port `18001:8001` (+ metrics `19101`)
- **Runtime Service:** `saraise-runtime` - Port `18002:8002` (+ metrics `19102`)
- **Policy Engine:** `saraise-policy-engine` - Port `18003:8003` (+ metrics `19103`)
- **Control Plane:** `saraise-control-plane` - Port `18004:8004` (+ metrics `19104`)

### Phase 4/5 Services (1 service)
- **Backend (Legacy):** `saraise-backend-phase1` - Port `18005:8005`

### Phase 6 Services (2 services)
- **Backend API:** `saraise-phase6-backend` - Port `18000:8000`
- **Frontend UI:** `saraise-phase6-frontend` - Port `15173:5173`

### Observability (3 services)
- **Prometheus:** `saraise-prometheus` - Port `19090:9090`
- **Grafana:** `saraise-grafana` - Port `13000:3000`
- **Jaeger:** `saraise-jaeger` - Port `116686:16686` (UI) + others

**Total:** 12 services in single docker-compose file

---

## Port Mapping Convention

**All external ports start with "1" prefix:**

| Service | Internal Port | External Port | URL |
|---------|--------------|---------------|-----|
| PostgreSQL | 5432 | 15432 | localhost:15432 |
| Redis | 6379 | 16379 | localhost:16379 |
| Auth Service | 8001 | 18001 | http://localhost:18001 |
| Runtime Service | 8002 | 18002 | http://localhost:18002 |
| Policy Engine | 8003 | 18003 | http://localhost:18003 |
| Control Plane | 8004 | 18004 | http://localhost:18004 |
| Backend (Phase 1) | 8005 | 18005 | http://localhost:18005 |
| Backend (Phase 6) | 8000 | 18000 | http://localhost:18000 |
| Frontend (Phase 6) | 5173 | 15173 | http://localhost:15173 |
| Prometheus | 9090 | 19090 | http://localhost:19090 |
| Grafana | 3000 | 13000 | http://localhost:13000 |
| Jaeger UI | 16686 | 116686 | http://localhost:116686 |

---

## Network Architecture

**Single Network:** `saraise-network` (bridge driver)

All 12 services communicate via the same network:
- Service discovery via container names
- No port conflicts
- Simplified networking

---

## Usage

### Start All Services
```bash
./scripts/docker/start-all.sh
```

**Or manually:**
```bash
docker-compose -f docker-compose.dev.yml up -d --build
```

### Stop All Services
```bash
./scripts/docker/stop-all.sh
```

**Or manually:**
```bash
docker-compose -f docker-compose.dev.yml down
```

### View Logs
```bash
# All services
docker-compose -f docker-compose.dev.yml logs -f

# Specific service
docker-compose -f docker-compose.dev.yml logs -f backend
docker-compose -f docker-compose.dev.yml logs -f frontend
```

### Check Status
```bash
docker-compose -f docker-compose.dev.yml ps
```

---

## Service Dependencies

### Startup Order
1. **Infrastructure:** postgres, redis (health checks)
2. **Phase 2:** auth, policy-engine (health checks)
3. **Phase 2:** runtime (depends on auth, policy-engine)
4. **Phase 2:** control-plane
5. **Phase 4/5:** backend-phase1 (depends on postgres, redis)
6. **Phase 6:** backend (depends on postgres, redis)
7. **Phase 6:** frontend (depends on backend)
8. **Observability:** prometheus, grafana (depends on prometheus), jaeger

---

## Environment Variables

### Default Ports (can be overridden via .env)
```env
POSTGRES_PORT=15432
REDIS_PORT=16379
BACKEND_PORT=18000
FRONTEND_PORT=15173
SECRET_KEY=<auto-generated>
```

---

## Verification

### Check All Containers
```bash
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}" | grep saraise
```

### Check Network
```bash
docker network inspect saraise-network --format '{{range .Containers}}{{.Name}} {{end}}'
```

### Test Services
```bash
# Backend (Phase 6)
curl http://localhost:18000/api/v1/ai-agents/health/

# Frontend
curl http://localhost:15173

# Auth Service
curl http://localhost:18001/health

# Prometheus
curl http://localhost:19090/-/healthy

# Grafana
curl http://localhost:13000/api/health
```

---

## Benefits

1. **Single File:** All services in one docker-compose file
2. **Single Network:** All services communicate via `saraise-network`
3. **Port Standardization:** Clear "1" prefix convention
4. **Simplified Management:** One command to start/stop everything
5. **Consistent Configuration:** Unified environment variables
6. **Easy Debugging:** All logs in one place

---

## Migration Notes

### Breaking Changes
- PostgreSQL external port: `5432` → `15432`
- Redis external port: `6379` → `16379`
- All Phase 2 service ports updated with "1" prefix
- Grafana port: `3000` → `13000`
- Prometheus port: `9090` → `19090`
- Jaeger UI port: `16686` → `116686`

### Update Required
- Database connection strings (if connecting from host)
- Redis connection strings (if connecting from host)
- Browser bookmarks
- API client configurations
- Environment variables
- Documentation references

---

## Files Created/Modified

### Docker Configuration
- `docker-compose.dev.yml` - Consolidated compose file (all services)
- `scripts/docker/start-all.sh` - Start script for all services
- `scripts/docker/stop-all.sh` - Stop script for all services

### Documentation
- `reports/DOCKER-CONSOLIDATION-COMPLETE-2026-01-05.md` - This document

---

## Quick Reference

### Service URLs
- **Frontend UI:** http://localhost:15173
- **Backend API (Phase 6):** http://localhost:18000
- **Backend (Phase 1):** http://localhost:18005
- **Auth Service:** http://localhost:18001
- **Runtime Service:** http://localhost:18002
- **Policy Engine:** http://localhost:18003
- **Control Plane:** http://localhost:18004
- **Prometheus:** http://localhost:19090
- **Grafana:** http://localhost:13000
- **Jaeger UI:** http://localhost:116686

### Health Checks
- Backend: http://localhost:18000/api/v1/ai-agents/health/
- Auth: http://localhost:18001/health
- Runtime: http://localhost:18002/health
- Policy Engine: http://localhost:18003/health
- Control Plane: http://localhost:18004/health

---

**Status:** ✅ COMPLETE

All services consolidated into single docker-compose file with unified network and port standardization.

---

**Last Updated:** January 5, 2026

