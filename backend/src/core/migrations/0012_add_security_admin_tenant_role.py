# Generated by Django 5.2.16 on 2026-07-26 15:24
#
# Tier-1 (non-destructive) reconciliation: re-adds the `security_admin` tenant_role
# choice that lived only in the removed divergent migration
# `0010_add_security_admin_tenant_role.py`. The DeleteModel operations for the legacy
# core Notification tables (data already migrated to the notifications module via
# notifications.0003_import_legacy_notifications) are deferred to a separate reviewed
# migration.

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0011_apply_typed_rls_to_notifications"),
    ]

    operations = [
        migrations.AlterField(
            model_name="userprofile",
            name="tenant_role",
            field=models.CharField(
                blank=True,
                choices=[
                    ("tenant_admin", "Tenant Admin"),
                    ("security_admin", "Security Administrator"),
                    ("tenant_user", "Tenant User"),
                ],
                db_index=True,
                help_text="Tenant-level role",
                max_length=50,
                null=True,
            ),
        ),
    ]
